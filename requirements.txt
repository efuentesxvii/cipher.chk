# Password Security Analyzer — Python dependencies
# Install with: pip install -r requirements.txt

# API framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Rate limiting
slowapi==0.1.9

# HTTP client (async, for HIBP calls)
httpx==0.27.0

# Data validation (ships with FastAPI but pinned for clarity)
pydantic==2.7.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6

# Security linting (used in CI)
bandit==1.7.8
